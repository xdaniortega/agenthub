import chalk from "chalk";

export async function runImportAgent(): Promise<void> {
    console.log(chalk.yellow("\n📦 Import Agent — coming soon!\n"));
    console.log(chalk.gray("This command will add ERC-8004 compliance to an existing project.\n"));
}
